plugins {
    id 'java-library'
}

dependencies {
    implementation 'org.example:utils:1.0-SNAPSHOT'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.8.1'
}

test {
    useJUnitPlatform()
}

tasks.named('compileJava') {
    dependsOn ':utils:publishToMavenLocal'
}
